Create table Recipes
(
	RecipeID int  AUTO_INCREMEN,
	RecipeName nvarchar(20),
	PRIMARY KEY (Personid)
);

Create table Ingredients
(
	RecipeID int  AUTO_INCREMENT,
	RecipeName nvarchar(20),
	PRIMARY KEY (Personid)
);